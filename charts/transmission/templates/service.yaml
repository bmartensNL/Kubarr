apiVersion: v1
kind: Service
metadata:
  name: {{ include "transmission.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "transmission.labels" . | nindent 4 }}
  annotations:
    {{- /* kubarr.io/base-path: Tells the Kubarr proxy that this app serves its UI at a subpath */}}
    {{- /* (e.g., /sonarr). When set, the proxy keeps the full path instead of stripping the app prefix. */}}
    {{- /* Only set this if the app is actually configured to serve from this path. */}}
    {{- if .Values.transmission.basePath }}
    kubarr.io/base-path: {{ .Values.transmission.basePath | quote }}
    {{- end }}
    {{- with .Values.transmission.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.transmission.service.type }}
  ports:
    - port: {{ .Values.transmission.service.port }}
      targetPort: {{ .Values.transmission.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "transmission.selectorLabels" . | nindent 4 }}
