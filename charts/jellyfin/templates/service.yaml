apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyfin.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "jellyfin.labels" . | nindent 4 }}
  annotations:
    {{- /* kubarr.io/base-path: Tells the Kubarr proxy that this app serves its UI at a subpath */}}
    {{- /* (e.g., /sonarr). When set, the proxy keeps the full path instead of stripping the app prefix. */}}
    {{- /* Only set this if the app is actually configured to serve from this path. */}}
    {{- if .Values.jellyfin.basePath }}
    kubarr.io/base-path: {{ .Values.jellyfin.basePath | quote }}
    {{- end }}
    {{- with .Values.jellyfin.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.jellyfin.service.type }}
  ports:
    - port: {{ .Values.jellyfin.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jellyfin.selectorLabels" . | nindent 4 }}
