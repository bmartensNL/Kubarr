apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyseerr.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "jellyseerr.labels" . | nindent 4 }}
  annotations:
    {{- /* kubarr.io/base-path: Tells the Kubarr proxy that this app serves its UI at a subpath */}}
    {{- /* (e.g., /sonarr). When set, the proxy keeps the full path instead of stripping the app prefix. */}}
    {{- /* Only set this if the app is actually configured to serve from this path. */}}
    {{- if .Values.jellyseerr.basePath }}
    kubarr.io/base-path: {{ .Values.jellyseerr.basePath | quote }}
    {{- end }}
    {{- with .Values.jellyseerr.service.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.jellyseerr.service.type }}
  ports:
    - port: {{ .Values.jellyseerr.service.port }}
      targetPort: {{ .Values.jellyseerr.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "jellyseerr.selectorLabels" . | nindent 4 }}
