apiVersion: v1
kind: Service
metadata:
  name: {{ include "jackett.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "jackett.labels" . | nindent 4 }}
  {{- with .Values.jackett.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.jackett.service.type }}
  ports:
    - port: {{ .Values.jackett.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jackett.selectorLabels" . | nindent 4 }}
