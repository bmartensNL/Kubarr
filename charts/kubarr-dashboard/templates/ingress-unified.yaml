{{- if and .Values.ingress.enabled .Values.oauth2.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "kubarr-dashboard.fullname" . }}-unified
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "kubarr-dashboard.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          # Auth endpoints (no authentication required)
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}
                port:
                  number: {{ $.Values.backend.service.port }}

          # OAuth2-proxy callback (no authentication required)
          - path: /oauth2
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}-oauth2-proxy
                port:
                  number: 4180

          # Dashboard frontend and API (no authentication required)
          - path: /dashboard
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}
                port:
                  number: {{ $.Values.frontend.service.port }}

          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}
                port:
                  number: {{ $.Values.backend.service.port }}

          # Media apps (authentication required via oauth2-proxy)
          - path: /apps
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}-oauth2-proxy
                port:
                  number: 4180

          # Root path to dashboard
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "kubarr-dashboard.fullname" $ }}
                port:
                  number: {{ $.Values.frontend.service.port }}
    {{- end }}
{{- end }}
