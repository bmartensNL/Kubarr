# Backend API service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubarr-dashboard.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "kubarr-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubarr-dashboard.selectorLabels" . | nindent 4 }}
---
# Frontend service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubarr-dashboard.fullname" . }}-frontend
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "kubarr-dashboard.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kubarr-dashboard.name" . }}-frontend
    app.kubernetes.io/instance: {{ .Release.Name }}
